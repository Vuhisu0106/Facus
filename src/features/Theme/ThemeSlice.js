// const darkModeState = JSON.parse(localStorage.getItem('darkMode'));

// const initialState = false || darkModeState;

// const theme = createSlice({
//     name: 'theme',
//     initialState,
//     reducers: {
//         toggleTheme: (state, action) => {
//             return !state;
//         },
//     },
// });

// const { reducer, actions } = theme;
// export const { toggleTheme } = actions;
// export default reducer;
